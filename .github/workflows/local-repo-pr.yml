name: Limit PRs on local repo only for E2E tests

on:
  pull_request:
    branches:
      - main

jobs:
  limit_main_pr:
    runs-on: ubuntu-latest
    steps:
      - uses: LukBukkit/action-pr-limits@v1.1.2
        with:
          denylist: |
            \:
