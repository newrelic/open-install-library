---

- name: Disable nginx-amplify permanently (if present)
  shell: "yum-config-manager --disable nginx-amplify"
  become: yes
  register: disable_amplify
  changed_when: disable_amplify.rc == 0
  failed_when: disable_amplify.rc != 0 and 'No matching repo to modify' not in disable_amplify.stderr
