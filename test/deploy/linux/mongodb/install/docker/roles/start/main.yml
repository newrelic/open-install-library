---

- fail:
    msg: "service_id is required"
  when: service_id is not defined

- debug:
    msg: Starting {{ service_id }} service

- name: Run MongoDB Docker container
  shell: "docker run --name test-mongo-instance -d mongo"
  become: yes
