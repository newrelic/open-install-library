---
- debug:
    msg: Install SQL Server for NRDOT MSSQL Testing on Debian/Ubuntu

- name: Import Microsoft GPG key
  shell: |
    curl https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc
  become: true

- name: Detect Ubuntu version and add appropriate repository
  shell: |
    # Get Ubuntu version
    UBUNTU_VERSION=$(lsb_release -rs)
    
    if [ "$UBUNTU_VERSION" = "22.04" ]; then
      # Ubuntu 22.04
      curl -fsSL https://packages.microsoft.com/config/ubuntu/22.04/mssql-server-2022.list | sudo tee /etc/apt/sources.list.d/mssql-server.list
    elif [ "$UBUNTU_VERSION" = "24.04" ]; then
      # Ubuntu 24.04
      curl -fsSL https://packages.microsoft.com/config/ubuntu/24.04/mssql-server-2022.list | sudo tee /etc/apt/sources.list.d/mssql-server.list
    else
      # Default to Ubuntu 22.04 repository for other versions
      curl -fsSL https://packages.microsoft.com/config/ubuntu/22.04/mssql-server-2022.list | sudo tee /etc/apt/sources.list.d/mssql-server.list
    fi
  become: true

- name: Update package list
  shell: "apt-get update -y"
  become: true

- name: Install SQL Server
  shell: "DEBIAN_FRONTEND=noninteractive apt-get install -y mssql-server"
  become: true
  environment:
    DEBIAN_FRONTEND: noninteractive

- name: Configure SQL Server with SA password
  shell: |
    MSSQL_SA_PASSWORD='YourStrong@Passw0rd' \
    MSSQL_PID='evaluation' \
    /opt/mssql/bin/mssql-conf -n setup accept-eula
  become: true
  environment:
    MSSQL_SA_PASSWORD: 'YourStrong@Passw0rd'
    MSSQL_PID: 'evaluation'

- name: Enable SQL Server Agent
  shell: "/opt/mssql/bin/mssql-conf set sqlagent.enabled true"
  become: true

- name: Restart SQL Server service
  shell: "systemctl restart mssql-server"
  become: true

- name: Wait for SQL Server to be ready
  shell: "sleep 10"
  become: true

- name: Verify SQL Server is running
  shell: "systemctl status mssql-server --no-pager"
  become: true
