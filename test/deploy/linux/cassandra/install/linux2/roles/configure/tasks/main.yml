---
- debug:
    msg: Install Cassandra reverse proxy

- name: install the Cassandra engine
  shell: "amazon-linux-extras install -y cassandra1"
  become: true

#in-progress

- name: Restart cassandra
  shell: "systemctl restart cassandra"
  become: yes
