---
- debug:
    msg: Install Cassandra

- name: Get latest packages info
  shell: "apt-get update -y"
  become: true

- name: Run Cassandra
  shell: "systemctl start cassandra"
  become: true

- name: Create user
  command: |
    cqlsh localhost -e "CREATE USER test WITH PASSWORD 'test';"
  become: true
